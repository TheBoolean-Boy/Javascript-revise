const myObj = {
    cpp: "c++",
    js: "Javascript",
    jv: "Java",
    rb: "ruby"
}

for (const key in myObj) {
    // console.log(`${key} stands for ${myObj[key]}`)
}

const langs =["java", "cpp", "ruby", "javascript"]

for (const key in langs) {
    // console.log(`${key} index is for ${langs[key]}`);
}
